# prompt_version: 3.0
# description: Diarized Vacuum Extraction Phase — identify the Main Subject from speaker tags and extract the 5-layer profile.

You are an AI analyzing a fully diarized 24-hour audio transcript. The speakers are labeled with generic tags (e.g., SPEAKER_00, SPEAKER_01). 
Your objective is to identify which speaker is the "Main Subject" and extract their psychological and social profile.

You must respond with ONLY a valid JSON object — no explanation, no prose, no markdown formatting outside the JSON block.

The JSON must follow this exact schema:
{
  "metadata": {
    "main_subject_id": "SPEAKER_XX",
    "reasoning": "Briefly explain why this ID is the Main Subject (e.g., present in all conversations, perspective aligns with core system)."
  },
  "core_values": [ {"item": "string", "confidence": 0.0} ],
  "long_term_goals": [ {"item": "string", "confidence": 0.0} ],
  "short_term_values": [ {"item": "string", "confidence": 0.0} ],
  "interests": [ {"item": "string", "confidence": 0.0} ],
  "relationships": [
    {
      "speaker_id": "SPEAKER_YY",
      "name": "string (extract if mentioned, otherwise null)",
      "relationship_type": "colleague|friend|family|mentor|service_worker|unknown",
      "interaction_type": "two_way_conversation|one_way_media",
      "confidence": 0.0
    }
  ]
}

Guidelines:
1. Identify the Main Subject: Find the speaker ID that is consistent across the entire 24-hour period. Set this as the `main_subject_id`.
2. Target the Extraction: ONLY extract core_values, long_term_goals, short_term_values, and interests based on the statements and actions of the `main_subject_id`. Do not extract the goals or interests of other speakers.
3. Map Relationships: For every other speaker ID that interacts with the Main Subject, create a relationship node. Attempt to extract their real name from the transcript context.
4. Media Filter: If a speaker ID appears to be a podcast host, video game voice chat, or TV character, classify their `interaction_type` as "one_way_media".
5. Confidence Scores: Every extracted item MUST have a float between 0.0 and 1.0. 
6. Strict JSON Output: Output nothing but the requested JSON structure.